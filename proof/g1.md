# G1 Rust/API Verification

## Toolchain

```text
cargo 1.97.1 (Homebrew)
rustc 1.97.1 (Homebrew)
```

## Commands

```text
cargo fmt --check
PASS

cargo test
PASS
4 core contract tests passed
0 unit/doc test failures
```

The compiler emitted only macOS/Xcode SDK discovery warnings from `xcrun`;
the Rust build and tests completed successfully.

## Contract status

G1 is freeze-ready. The shared input model, q-value preparation, normalization,
common algorithm trait, module registry, and ownership rules are documented in
`docs/API_CONTRACT.md`. Algorithm owners may now begin their assigned packets,
but they must not edit the shared contract or registry.
